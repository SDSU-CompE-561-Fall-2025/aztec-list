# ============================================================================
# FRONTEND ENVIRONMENT VARIABLES (.env.local)
# ============================================================================
# This file configures the Next.js frontend application.
# Copy this file to .env.local for local development.
#
# IMPORTANT: Next.js requires NEXT_PUBLIC_ prefix for variables accessible in browser.
# Variables without this prefix are only available server-side.
# ============================================================================

# API Configuration
# Backend API base URL (include the full path with version)
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000/api/v1

# Static files URL (for uploaded images, etc.)
# Should match the backend server URL
NEXT_PUBLIC_STATIC_BASE_URL=http://127.0.0.1:8000

# App Configuration
NEXT_PUBLIC_APP_NAME="Aztec List"

# Testing Configuration
# For E2E tests with admin functionality, you must enable TEST_MODE in the backend
# Set TEST__TEST_MODE=true in backend/.env to enable test-only endpoints
# This allows tests to promote users to admin role for testing admin features
